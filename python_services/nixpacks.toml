[phases.setup]
nixPkgs = ["python39", "python39Packages.pip", "gcc", "cmake"]

[phases.install]
cmds = [
    "python -m pip install --upgrade pip",
    "pip install --no-cache-dir -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "python eye_tracking_service.py"
