# Environment Variables for Railway Deployment
# Copy these to your Railway project's environment variables

# REQUIRED: Disable server-side camera (Railway has no camera)
CAMERA_ENABLED=false

# PORT is automatically set by Railway, don't override
# PORT=8000

# CORS Configuration - Set to your PHP app URL or use * for development
ALLOWED_ORIGINS=*

# Optional: Tracking save URL (if different from default)
# TRACKING_SAVE_URL=https://your-php-app.railway.app/user/database/save_enhanced_tracking.php
